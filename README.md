# YurService Microfrontend

Микросервис (микрофронтенд) для страницы "ЮрСервисы".

## Описание

Этот микросервис содержит страницу каталога юридических сервисов, которая может быть интегрирована в основное приложение. Микросервис работает изолированно и может быть развернут отдельно от основного приложения.

## 🚀 Быстрый старт

### Установка

Зависимости устанавливаются автоматически при `npm install` в корне проекта благодаря npm workspaces.

Если нужно установить зависимости только для микросервиса:

```bash
cd microfrontend-yurservice
npm install
```

### Запуск

#### Локальная разработка

```bash
# Из корня проекта (рекомендуется)
npm run dev

# Или отдельно микросервис
cd microfrontend-yurservice
npm run dev
```

Микросервис будет доступен на:
- `http://localhost:5173/yurservice` при запуске из корня
- `http://localhost:3001` при отдельном запуске

#### Сборка

```bash
# Сборка основного приложения (микросервис включен автоматически)
npm run build

# Или отдельно микросервис
cd microfrontend-yurservice
npm run build
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Микросервис автоматически использует эти переменные из основного проекта.

## 🔗 Интеграция

Микросервис автоматически интегрирован в основное приложение через импорт в `src/App.tsx`:

```typescript
import { YurServicePage } from "../microfrontend-yurservice/src/components/yurservice/YurServicePage";
```

## 📁 Структура

```
microfrontend-yurservice/
├── src/
│   ├── components/
│   │   └── yurservice/      # Компоненты YurService
│   │       ├── YurServicePage.tsx
│   │       ├── ResourceCard.tsx
│   │       └── RegionSelect.tsx
│   ├── hooks/               # React hooks
│   │   └── use-yurservice-data.ts
│   ├── lib/                 # Утилиты
│   │   └── resource-mapper.ts
│   ├── types/               # TypeScript типы
│   │   ├── database.ts
│   │   └── resource.ts
│   └── main.tsx            # Точка входа
├── package.json
└── vite.config.ts
```

## 🎯 Использование shared кода

Микросервис использует общие компоненты, хуки и утилиты из `shared-src/` через алиас `@shared`:

```typescript
// UI компоненты
import { Button } from '@shared/components/ui/button'
import { Card } from '@shared/components/ui/card'
import { Input } from '@shared/components/ui/input'

// Хуки
import { useToast } from '@shared/hooks/use-toast'

// Утилиты
import { cn } from '@shared/lib/utils'
import { supabase } from '@shared/lib/supabase'
```

## ✨ Особенности

- ✅ Автоматическая установка зависимостей через npm workspaces
- ✅ Использует общие зависимости из корневого проекта через peerDependencies (без дублирования)
- ✅ Использует shared компоненты из `shared-src/`
- ✅ Работает напрямую с БД через Supabase
- ✅ Изолированная сборка и деплой
- ✅ Оптимизированная структура зависимостей

## 📚 Дополнительная информация

- [INTEGRATION.md](./INTEGRATION.md) - детали интеграции
- [../INTEGRATION_GUIDE.md](../INTEGRATION_GUIDE.md) - общее руководство по интеграции
- [../START.md](../START.md) - инструкция по запуску
